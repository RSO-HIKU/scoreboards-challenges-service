apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "scoreboards-challenges-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "scoreboards-challenges-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "scoreboards-challenges-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "scoreboards-challenges-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: scoreboards-challenges-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.clusterIP.port }}