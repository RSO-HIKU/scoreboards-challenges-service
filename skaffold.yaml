apiVersion: skaffold/v4beta8
kind: Config
metadata:
  name: scoreboards-challenges-service
build:
  local:
    push: false
  tagPolicy:
    sha256: {}
  artifacts:
    - image: scoreboards-challenges-service
      context: .
      docker:
        dockerfile: Dockerfile
    - image: scoreboards-challenges-service-migrator
      context: .
      docker:
        dockerfile: Dockerfile.migrator
deploy:
  helm:
    releases:
      - name: scoreboards-challenges-service
        chartPath: helm
        namespace: dev
        createNamespace: true
        valuesFiles:
          - helm/values-dev.yaml
        setValueTemplates:
          image.repository: "{{.IMAGE_REPO_scoreboards_challenges_service}}"
          image.tag: "{{.IMAGE_TAG_scoreboards_challenges_service}}"
          flyway.image.repository: "{{.IMAGE_REPO_scoreboards_challenges_service_migrator}}"
          flyway.image.tag: "{{.IMAGE_TAG_scoreboards_challenges_service_migrator}}"
        wait: true
portForward:
  - resourceType: service
    resourceName: scoreboards-challenges-service-nodeport
    namespace: dev
    port: 8089
    localPort: 8089
profiles:
  - name: minikube
    activation:
      - kubeContext: minikube